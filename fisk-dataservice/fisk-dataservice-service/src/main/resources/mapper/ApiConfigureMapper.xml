<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fisk.dataservice.mapper.ApiConfigureMapper">

    <select id="queryData" resultType="java.util.Map">
       ${splitSql} limit #{currentPage},#{pageSize}
    </select>

    <select id="filter" resultType="com.fisk.dataservice.vo.DownSystemQueryVO">
      SELECT
        id,
        down_system_name,
        system_info,
        user_name,
        `password`,
        create_time,
        update_time
    FROM
        `configure_user`
    WHERE
        del_flag = '1'
    <if test="query.where !=null and query.where.length()>0 ">
        ${query.where}
    </if>
    </select>
</mapper>