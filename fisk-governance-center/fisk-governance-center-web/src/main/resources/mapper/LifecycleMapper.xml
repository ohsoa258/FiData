<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fisk.datagovernance.mapper.dataquality.LifecycleMapper">
    <select id="getAll" resultType="com.fisk.datagovernance.vo.dataquality.lifecycle.LifecycleVO">
        SELECT
        t1.id,
        template_id,
        datasource_id,
        datasource_type,
        module_name,
        table_name,
        field_name,
        recovery_date,
        remind_date,
        is_backup,
        check_emptytb_day,
        check_refreshtb_day,
        check_consanguinity_day,
        run_time_cron,
        table_state,
        module_state,
        t2.templaten_name,
        t2.template_modules,
        t2.template_type,
        t2.template_desc,
        t1.create_time,
        t1.create_user
        FROM
        tb_lifecycle_module t1
        LEFT JOIN tb_template_config t2 ON t1.template_id = t2.id
        WHERE
        t1.del_flag = 1
        AND table_name = #{tableName,jdbcType=VARCHAR}
        <if test="keyword != null and keyword != '' ">
            AND module_name LIKE concat('%', #{keyword}, '%')
        </if>
    </select>
</mapper>