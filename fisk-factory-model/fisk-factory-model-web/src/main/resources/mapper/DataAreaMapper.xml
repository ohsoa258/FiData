<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fisk.datamodel.mapper.DataAreaMapper">
    <select id="queryFilter" resultType="com.fisk.datamodel.dto.DataAreaDTO">
        select a.id,
        a.business_id,
        b.business_name,
        a.data_name,
        a.data_des
        from tb_area_data a
        left join `tb_area_business` as b
        on a.businessid = b.id
        where a.del_flag=1
        <if test="query.where !=null and query.where.length()>0 ">
            ${query.where}
        </if>
    </select>
</mapper>